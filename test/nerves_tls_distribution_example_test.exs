defmodule NervesTlsDistributionExampleTest do
  use ExUnit.Case
  doctest NervesTlsDistributionExample

  test "greets the world" do
    assert NervesTlsDistributionExample.hello() == :world
  end
end
